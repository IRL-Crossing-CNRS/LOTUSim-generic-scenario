{
  "_comment": [
    "A hull-only xdyn vessel with no Allocation task at all -- same pattern",
    "as fremm_drift_only.json. mineConfig.yaml has no 'controlled forces'/",
    "'commands' section (confirmed by grepping the file), so an empty",
    "command set is harmless and the vessel drifts correctly under",
    "hydrodynamics + its own embedded 'ekman current' model. Only",
    "Navigation runs, for telemetry -- there is nothing to command.",
    "",
    "Unlike Fremm/WAMV/commando/pha, this is Mine's only mode, permanently:",
    "by design it never has a Kinematic-controllable alternative in any",
    "scenario."
  ],
  "world_file": "energy.world",
  "renderer_unity": true,
  "agents": [
    {
      "id": "mine_drift_only",
      "class": "Mine",
      "nb_agents": 1,
      "xdyn": true,
      "spawn": { "x": 0.0, "y": 0.0, "z": -10.0, "yaw": 0.0 },
      "tick_rate_hz": 1.0,
      "missions": [
        { "id": "navigation", "type": "action", "task": "navigation", "params": {} }
      ]
    }
  ]
}
